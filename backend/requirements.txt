# Mech Platform — Python Dependencies
# Install: pip install -r requirements.txt
# Python version: 3.11 (set via PYTHON_VERSION env var on Render)

# ── Core framework ────────────────────────────────────────────
Flask==3.0.3
Flask-SocketIO==5.3.6
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-CORS==4.0.1

# ── WSGI server + async for WebSocket ────────────────────────
# eventlet is required for Flask-SocketIO on gunicorn free plan.
# Do NOT use gevent — incompatible with Flask-SocketIO 5.x.
gunicorn==22.0.0
eventlet==0.36.1

# ── Database ──────────────────────────────────────────────────
SQLAlchemy==2.0.30
psycopg2-binary==2.9.9      # binary build — no libpq-dev needed on Render

# ── Validation & serialization ────────────────────────────────
marshmallow==3.21.3

# ── Security ──────────────────────────────────────────────────
Werkzeug==3.0.3

# ── HTTP ──────────────────────────────────────────────────────
requests==2.32.3

# ── Excel (emergency contacts spreadsheet) ───────────────────
# Required by emergency_routes.py to read emergency_contacts.xlsx
openpyxl==3.1.2

# ── Environment ───────────────────────────────────────────────
python-dotenv==1.0.1

# ── SMS (Africa's Talking — uncomment when SMS is enabled) ────
# africastalking==1.2.6

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.2
pytest-flask==1.3.0
